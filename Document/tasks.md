# Town シーン実装タスク

以下は Town シーンの初期設計と実装タスクです。小さなステップに分けて進めます。

1. シーン構成（完了: ベースノードを作成）
   - ルート: `Town` (Node2D)
   - 地形レイヤー: `Town#地形` (TileMapLayer) — 既存の `TownTiles.tres` を参照
   - オブジェクトコンテナ: `Objects` (Node2D)
     - `Props` (Node2D)
     - `NPCs` (Node2D)
   - プレイヤースポーン: `PlayerSpawn` (Position2D)
   - カメラ: `Camera2D` (Camera2D) — `current = true` を設定（開発時のみ）
   - ナビゲーション領域: `NavigationRegion2D` (NavigationRegion2D)

2. 必要なスクリプト / 接続
   - プレイヤースポーンから Main シーンへスポーン処理を接続（スクリプトで設定）
   - NPC の親ノードにシグナル接続用のプレースホルダ（将来的に会話や AI を繋ぐ）

3. テスト
   - Godot エディタで Town シーンを開き、TileMap のタイルが表示されることを確認
   - プレイヤースポーン位置へプレイヤーをインスタンスしてカメラ追従を確認

4. 次のステップ（実装フェーズ）
   - NPC システム（会話/パスファインド）の設計
   - Props の配置と衝突設定
   - シーンストリーミング / パフォーマンス最適化（必要なら）

実施者: 自動化エージェント / ユーザ
日付: 2025-11-07
# 実装タスク — 最小プロジェクト構成 (自動生成)

作成日: 2025-11-03

今回追加したタスク:

- 最小シーン作成: `scenes/Main.tscn` を作成し、C# スクリプトを割り当てた。
- C# スクリプト追加: `scripts/Main.cs` を追加し、エントリポイントのログ出力を実装した。
- README: 最小セットアップと実行手順を `README.md` に記載した。

次の推奨タスク:

1. Godot エディタでプロジェクトを開き、Mono のソリューション生成を行ってビルド確認をする。
2. CI（GitHub Actions 等）で `dotnet build` を動かすワークフローの追加を検討する。
3. 必要に応じて `project.godot` の MainScene 設定を更新し、エディタ起動時に自動で開くようにする。
